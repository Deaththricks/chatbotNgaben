# Ngaben chatbot — Python deps. Install into the rasa_bot venv:
#   uv pip install --python .\rasa_bot\Scripts\python.exe --override overrides.txt -r requirements.txt
#
# Rasa 3.6.x requires Python 3.10 (not 3.11).

rasa==3.6.1
rasa-sdk==3.6.1

# Knowledge graph
neo4j==5.14.0

# Action-server helpers
rapidfuzz>=3.0
python-dotenv>=1.0

# LLM layer (coreference resolution + answer synthesis) — Qwen 2.5 via a local
# Ollama server, replacing the earlier Groq rephrase step (see PROJECT_CONTEXT.md).
langchain-core>=1.6
langchain-ollama>=1.1
